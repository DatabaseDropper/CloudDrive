import React from 'react';

import Layout_NoAuth from '../../Layouts/NoAuth';

const Page_Auth_Login = (props) => {
    return (
        <Layout_NoAuth pageTitle="Logowanie">
        </Layout_NoAuth>
    );
}

export default Page_Auth_Login;