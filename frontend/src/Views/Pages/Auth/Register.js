import React from 'react';

import Layout_NoAuth from '../../Layouts/NoAuth';

const Page_Auth_Register = (props) => {
    return (
        <Layout_NoAuth pageTitle="Rejestracja">
        </Layout_NoAuth>
    );
}

export default Page_Auth_Register;